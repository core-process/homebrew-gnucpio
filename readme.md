# Tap for GNU flavor of cpio

Run `brew install core-process/gnucpio/gnucpio` to install the GNU flavor of `cpio`. The GNU flavor of `cpio` can append files to existing archives in contrast to the BSD variant.
